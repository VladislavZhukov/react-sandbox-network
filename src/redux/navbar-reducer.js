let initialState = {
  friends: [
    { id: 1, name: "Viktor" },
    { id: 2, name: "Sveta" },
    { id: 3, name: "Jojo" },
  ],
};

const navBarReducer = (state = initialState, action) => {
  return state;
};

export default navBarReducer;
